# Boot on the Intel iGPU even when Apple's firmware left the display mux on the
# NVIDIA GPU (macOS resets gpu-power-prefs). See /usr/local/sbin/gmux-igd-hook.
install i915 /usr/local/sbin/gmux-igd-hook i915 $CMDLINE_OPTS
install nouveau /usr/local/sbin/gmux-igd-hook nouveau $CMDLINE_OPTS
